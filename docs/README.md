# docs/

Reserved for supplementary project documentation (e.g. generated API docs,
architecture diagrams) produced as the application grows.

The authoritative requirements and business rules live in
`ChildcareBenefit_Codex_Documentation/` at the repository root — that
folder is the source of truth and is not duplicated here.
