# Frontend Tests

No frontend test framework is set up yet. Increment 1 only required the
frontend to install and build successfully; a test runner (Vitest + React
Testing Library is the natural fit for this Vite + React stack) will be
added alongside the first feature that has meaningful UI behavior to test,
starting with Employee Login.
